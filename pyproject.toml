[project]
name = "rigger"
version = "0.1.0"
description = "AI-driven investment research assistant"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=13.7",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "sqlmodel>=0.0.21",
    "openai>=1.35",
    "httpx>=0.27",
    "apscheduler>=3.10",
    "jinja2>=3.1",
    "yfinance>=0.2.40",
    "pandas>=2.2",
    "numpy>=1.26",
    "tomli-w>=1.0",
    "feedparser>=6.0",
    "textual>=8.2.8",
]

[project.scripts]
rig = "rigger.tui.app:run_tui"

[project.entry-points."rigger.plugins"]
us_market = "rigger.plugins.markets.us:USMarket"
asx_market = "rigger.plugins.markets.asx:ASXMarket"
yfinance_data = "rigger.plugins.data.yfinance:YFinanceData"
asx_announcements = "rigger.plugins.data.asx_announcements:ASXAnnouncements"
sec_edgar = "rigger.plugins.data.sec_edgar:SECEdgar"
yfinance_calendar = "rigger.plugins.data.yfinance_calendar:YFinanceCalendar"
rss = "rigger.plugins.data.rss:RSSData"

[project.entry-points."rigger.targets"]
company = "rigger.plugins.targets.tickers:CompanyTarget"
sector = "rigger.plugins.targets.tickers:SectorTarget"
industry = "rigger.plugins.targets.tickers:IndustryTarget"
theme = "rigger.plugins.targets.tickers:ThemeTarget"
market = "rigger.plugins.targets.tickers:MarketTarget"
tickers = "rigger.plugins.targets.tickers:LegacyTickersTarget"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.12"
strict = true

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "pytest>=9.1.1",
    "respx>=0.23.1",
    "ruff>=0.16.7",
]
